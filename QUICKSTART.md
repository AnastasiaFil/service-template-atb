# Быстрый старт 🚀

## Запуск приложения одной командой

```bash
make run
```

Эта команда:
✅ Проверит и запустит Docker  
✅ Поднимет PostgreSQL и Oracle  
✅ Соберет проект  
✅ Запустит приложение  
✅ Откроет Swagger UI в браузере  

**Swagger UI откроется автоматически!**

---

## Что делать дальше?

### 1. Проверьте API в Swagger

Swagger UI откроется автоматически, но если этого не произошло:

**http://localhost:8080/swagger-ui/index.html**

### 2. Протестируйте endpoints

#### Попробуйте GET-запросы (уже есть данные):

- **GET /api/oracle-users** - Список пользователей Oracle (5 записей)
- **GET /api/oracle-users/grants** - Список прав доступа (5 записей)
- **GET /api/oracle-users/roles** - Список ролей (5 записей)
- **GET /api/postgres-users** - Список пользователей PostgreSQL

#### Попробуйте POST-запросы:

1. Откройте endpoint **POST /api/oracle-users**
2. Нажмите "Try it out"
3. Вставьте JSON:
```json
{
  "name": "John Doe",
  "date": "1990-01-01",
  "sex": "M"
}
```
4. Нажмите "Execute"

### 3. Остановка приложения

Нажмите `Ctrl+C` в терминале, где запущено приложение, затем:

```bash
make stop
```

---

## Другие полезные команды

```bash
make logs           # Посмотреть логи контейнеров
make test           # Запустить тесты
make swagger        # Открыть Swagger UI
make help           # Показать все команды
```

---

## Что внутри?

### Автоматически создаются:

**Oracle Users (5 записей):**
- Ivan Petrov (M, 1990-05-15)
- Maria Sidorova (F, 1985-08-22)
- Alexey Ivanov (M, 1992-12-10)
- Elena Volkova (F, 1988-03-30)
- Dmitry Sokolov (M, 1995-07-18)

**Oracle Grants (5 записей):**
- READ_ACCESS
- WRITE_ACCESS
- DELETE_ACCESS
- EXECUTE_ACCESS
- ADMIN_ACCESS

**Oracle Roles (5 записей):**
- USER
- DEVELOPER
- ANALYST
- MANAGER
- ADMINISTRATOR

---

## Troubleshooting

**Oracle долго стартует?**  
Это нормально, первый запуск может занять 2-3 минуты.

**Docker не запустился?**  
Запустите Docker Desktop вручную и повторите `make run`.

**Порт 8080 занят?**  
Остановите другие приложения на порту 8080.

---

**Готово! Приятной работы! 🎉**
